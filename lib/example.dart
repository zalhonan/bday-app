import 'services/events_randomizer.dart';
import 'models/event.dart';
import 'package:intl/intl.dart';

void main() {
  // List<DateTime> listDates = [currDt, mydate1, mydate0];

  // print(listDates);
  // listDates.sort((a, b) => b.compareTo(a));
  // print(listDates);

  // print(currDt.compareTo(mydate1));
  // print("--------------");
  //
  // print(currDt.difference(mydate0).inDays);
  // print(currDt.difference(mydate1).inDays);
  // print(currDt.difference(mydate2).inDays);

  var d1 = "2021-05-03";
  var d2 = "1922-05-04";
  var d3 = "2000-05-05";
  var d4 = "2021-05-06";
  var d5 = "2000-05-02";

  print('-----------');
  // print(inDays(daysBetweenToday(DateTime.parse(d1))));
  // print(inDays(daysBetweenToday(DateTime.parse(d2))));
  // print(inDays(daysBetweenToday(DateTime.parse(d3))));
  // print(inDays(daysBetweenToday(DateTime.parse(d4))));
  // print(inDays(daysBetweenToday(DateTime.parse(d5))));

  // var randomizer =
  //     EventsRandomizer(eventsAmount: 30, startYear: 1900, endYear: 2023);

  // List<Event> eventsPack = randomizer.getEvents();

  // for (Event event in eventsPack) {
  //   print(event);
  // }
}
