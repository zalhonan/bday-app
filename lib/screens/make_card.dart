import 'package:flutter/material.dart';

class MakeCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Make Card here yo"),
      ),
    );
  }
}
